apiVersion: v1
kind: Service
metadata:
  labels:
    app: speedserver
#    version: v1
  name: demo-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
    name: http-port
  selector:
    app: speedserver
  type: ClusterIP
